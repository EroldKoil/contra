function resize() {
  //let WH = game.getWH();
  /*let width = window.innerWidth;
  let height = window.innerHeight;
  let wNorm = 256;
  let hNorm = 224;

  if (width / height > wNorm / hNorm) {
    width = wNorm / hNorm * height;
  } else {
    height = width / wNorm * hNorm
  }

  pjs.system.resize(width, height);
  contra.options.zoom = width / wNorm;
  pjs.camera.scale(p(contra.options.zoom, contra.options.zoom));

  console.log(width, 'width', height, 'height');
  console.log('zoom', contra.options.zoom);*/
  // contra
}